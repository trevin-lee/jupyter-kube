# Force npm to install all optional dependencies (platform-specific binaries)
force=true
optional=true

# Ensure npm uses exact versions from lockfile
save-exact=true

# Use shared lockfile in workspaces
lockfile-version=3 